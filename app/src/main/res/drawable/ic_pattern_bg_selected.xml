<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" 
    >

    <!-- 圆角 -->
 <!--    <corners
        android:radius="9dp"
    /> -->
 <!-- 设置圆角半径 -->
    <!-- 渐变 -->
   <!--  <gradient
        android:angle="45"
        android:centerColor="@android:color/black"
        android:centerX="0"
        android:centerY="0"
        android:endColor="@android:color/black"
        android:gradientRadius="90"
        android:startColor="@android:color/white"
        android:type="radial"
        android:useLevel="true" /> -->
    <!-- 间隔 -->
 <!--    <padding
        android:bottom="0dp"
        android:left="0dp"
        android:right="0dp"
        android:top="0dp" /> -->
 	<!-- 各方向的间隔 -->
 
    <!-- 大小 -->
 <!--    <size
        android:height="50dp"
        android:width="50dp" /> -->
 	<!-- 宽度和高度 -->
 	
    <!-- 填充 -->
      <solid android:color="@color/color_pattern_bg_selected" />   
    <!-- 填充的颜色 -->
    
    <!-- 描边 -->
     <!--  <stroke
         android:width="1px"
        android:color="@color/color_divider" />  -->

</shape>